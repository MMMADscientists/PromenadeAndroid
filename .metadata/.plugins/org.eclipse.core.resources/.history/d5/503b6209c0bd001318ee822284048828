<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.promenadevt.android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!-- Login Activity -->
        <activity
            android:label="@string/app_name"
            android:name="com.promenadevt.LoginActivity">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
 
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- homes activity -->
        <activity
            android:label="Properties"
            android:name="com.promenadevt.HomesActivity">
        </activity>
 
        <!--  Register Activity -->
        <activity
            android:label="Register New Account"
            android:name="com.promenadevt.RegisterActivity">
        </activity>
        <!-- Rooms Activity -->
        <activity
            android:label="Choose Room"
            android:name="com.promenadevt.RoomsActivity">
        </activity>
        <!-- Edit Activity -->
        <activity
            android:label="Edit Room"
            android:name="com.promenadevt.EditActivity">
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera"
                  android:required="true" />
    
</manifest>
